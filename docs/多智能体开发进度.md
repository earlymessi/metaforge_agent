# MetaForge MES 多智能体 — 开发进度总览

> **主文档**：[`智能体功能清单.md`](智能体功能清单.md) · [`docs/README.md`](README.md)  
> 设计初稿（历史）：[`superpowers/specs/2026-05-29-mes-multi-agent-design.md`](superpowers/specs/2026-05-29-mes-multi-agent-design.md)  
> 实施计划（已归档）：[`archive/plans/2026-05-29-mes-multi-agent.md`](archive/plans/2026-05-29-mes-multi-agent.md)  
> 更新日期：2026-05-31（6 Agent；pipeline 已合并入 scheduling）

---

## 1. 一句话结论

**规则版多智能体骨架已落地（Phase 0–4）**；**Phase 5.1–5.2 GLM 已实现**（排程意图 + Router + 事件解析，规则回退）。MCP 仍暂缓。

---

## 2. 访问方式（避免混淆）

| 方式 | 地址 | 说明 |
|------|------|------|
| **推荐（交付/日常）** | http://127.0.0.1:8000/new-ui/ | 后端托管 Vue 构建产物，路由 base 为 `/new-ui` |
| 根路径 | http://127.0.0.1:8000/ | 同样返回 `dist/index.html`，部分前端路由建议走 `/new-ui` |
| 开发热更新（可选） | http://127.0.0.1:5173/ | 仅 `npm run dev` 时；**非必须**，与 8000 二选一即可 |

启动：`cd tests && python main.py`（需 MongoDB）。前端改动后：`cd frontend && npm run build`。

---

## 3. 架构（当前实现）

```text
用户 / 前端
    │
    ▼
POST /api/orchestrator/run  ──► Intent Router（关键词 / intent）
    │                              │
    │                              ▼
    │                    唯一主责 Agent（6 选 1）
    │                              │
    │                              ▼ Plan-and-Solve（GLM 优先 + 规则回退）
    │                    白名单 Tool 调用链
    │
    └── 也可直连 POST /api/agents/{id}/run
```

**原则（与设计一致）：**

- 一个用户意图 → **一个**业务 Agent（Router 不串多 Agent）
- Agent 薄层；重计算在 Tool / 现有 `compare_solvers`、events 服务
- Tool 先行；LLM 仅预留 schema（`GET /api/tools/registry`）

---

## 4. 六个业务 Agent 状态

| Agent ID | 场景 | 状态 | API | 主要 Tool |
|----------|------|------|-----|-----------|
| `scheduling` | 智能排程 + 落库 HITL | ✅ | `POST /api/agents/scheduling/run` | `parse_intent`, `run`, `load_plan`, `assess`, `propose_persist` |
| `events` | 异常重排（A） | ✅ | `POST /api/agents/events/run` | `parse_event`, `check_insert_job`, `reschedule`, `explain_impact` |
| `kitting` | 齐套顾问（B） | ✅ | `POST /api/agents/kitting/run` | `material.*`, `kitting.build_report` |
| `commitment` | 交期承诺（C） | ✅ | `POST /api/agents/commitment/run` | `delivery.assess`, `customer_script` |
| `whatif` | 方案对比（D） | ✅ | `POST /api/agents/whatif/run` | `compare.variants` |
| `plans` | 计划管理（F） | ✅ | `POST /api/agents/plans/run` | `data.create/bind/list/...` |

兼容：`POST /api/agents/pipeline/run` → 转发 scheduling（`persist_after=true`）。

元数据：`GET /api/agents/registry`（6 条，`implemented: true`）。

遗留兼容：`POST /api/agent/schedule`（原 `SchedulingAgent` 自然语言排程）仍保留。

---

## 5. Tool 层清单（已注册）

| Tool | 说明 |
|------|------|
| `scheduling.parse_intent` | 自然语言 → 算法/权重/策略 |
| `scheduling.run` | 封装 `compare_solvers` |
| `material.check_static` | BOM 静态齐套 |
| `material.predict` | 排程后物料仿真 |
| `delivery.assess` | 交期风险评估 |
| `delivery.customer_script` | 对客户话术模板 |
| `delivery.explain_impact` | 重排影响解读 |
| `events.parse_event` | 规则解析 8 类事件 |
| `events.reschedule` | 统一重排（`event_reschedule` 服务） |
| `kitting.build_report` | 齐套报告聚合 |
| `compare.variants` | 多方案 what-if 对比 |
| `data.load_plan` | 加载计划工单上下文 |
| `data.propose_persist` | HITL：生成 confirm_token |
| `data.confirm_persist` | 校验 token（写库走 API） |

目录：`GET /api/tools/registry`。

---

## 6. 分阶段完成情况

### Phase 0 — Tool 基础 ✅

- `tools/base.py`、`registry.py`、`load_all.py`
- `scheduling` / `material` / `delivery` Tool
- `utils/problem_builder.py`（从 main 抽取工单构建）
- `GET /api/tools/registry`

### Phase 1 — Agent 骨架 + Router ✅

- `agents/base.py`（`AgentRequest` / `AgentResponse` / `PlanStep`）
- `scheduling`、`commitment` Agent
- `orchestrator/router.py`、`GET /api/agents/registry`
- `POST /api/orchestrator/run`

### Phase 2 — kitting + events ✅

- `kitting` Agent + `kitting.build_report`
- `events` Agent + `events.parse_event` / `events.reschedule`
- `services/event_reschedule.py` + `utils/event_mutations.py`
- 扩展 REST：`planned_downtime`、`material_delay`、`priority_change`、`order_cancel`、`quantity_change`

### Phase 3 — whatif + 排程落库 HITL ✅

- `compare.variants` + `whatif` Agent
- `services/persist_store.py`（15 分钟 token）
- `POST /api/db/propose_save`、`POST /api/db/confirm_save`
- 落库并入 **scheduling**（`pending_confirm`；原 pipeline 已废弃）
- 前端：`PersistConfirmDialog.vue` + APS「排程并待确认落库」
- `npm run build` → `tests/templates/dist`

### Phase 4 — 编排收尾 ✅

- [x] `orchestrator/session.py`（内存 Session，`artifacts` / `context` 续跑，TTL 1h）
- [x] `POST /api/orchestrator/run` 响应带 `session_id`；请求 `context.session_id` 可续跑
- [x] `GET /api/orchestrator/session/{session_id}` 查询会话摘要
- [x] Orchestrator 支持 scheduling + `confirm_token` 确认落库
- [x] `tests/test_orchestrator_session.py`、`tests/test_orchestrator_e2e.py`

### Phase 5.1 — GLM scheduling 解析 ✅

- `orchestrator/llm_client.py`、`scheduling/llm_intent.py`、`scheduling/resolve_intent.py`
- `scheduling.parse_intent` + `/api/agent/schedule` 共用；`GET /api/llm/status`
- APS 自然语言输入 + 预览解析；`LLM_ENABLED=1` + `ZHIPU_API_KEY` 启用

### Phase 5.2 — Router + events GLM ✅

- `orchestrator/llm_router.py`、`resolve_agent_route()`（`router_planner` 写入 orchestrator 响应）
- `events/llm_event.py`、`events/resolve_event.py` → `events.parse_event` 共用
- 环境变量与 5.1 相同：`LLM_ENABLED=1` + `.env` 中 `ZHIPU_API_KEY`

### Phase 5.3 — Agent LLM Plan ✅

- `orchestrator/llm_plan.py`：`scheduling`、`events` 默认启用（`LLM_PLAN_AGENTS`）
- `build_rule_plan` + `plan_planner`（`llm` / `rule` / `rule_fallback`）

### Phase 5.4 — 智能助手 UI ✅

- `frontend/src/views/AgentChatView.vue` → `/new-ui/assistant`
- 调用 `POST /api/orchestrator/run`，会话 `session_id` 续跑，HITL 确认落库

### Phase 6 — Session/HITL Mongo + MCP 骨架 ✅

- `SESSION_STORE=mongo`：`orchestrator_sessions`、`hitl_pending` 集合
- `orchestrator/mcp_adapter.py` + `GET /api/mcp/status`（`MCP_ENABLED=0` 默认关）
- 测试默认 `SESSION_STORE=memory`（`tests/conftest.py`）

---

## 7. 事件 / 重排 API（REST，与 events Agent 共用内核）

| 接口 | 事件类型 |
|------|----------|
| `POST /api/events/insert_order` | 插单 |
| `POST /api/events/machine_breakdown_reschedule` | 设备故障 |
| `POST /api/events/due_date_reschedule` | 改交期 |
| `POST /api/events/planned_downtime` | 计划停机 |
| `POST /api/events/material_delay` | 到料延迟 |
| `POST /api/events/priority_change` | 优先级变更 |
| `POST /api/events/order_cancel` | 撤单 |
| `POST /api/events/quantity_change` | 数量变更 |

---

## 8. 测试

```powershell
cd tests
$env:PYTHONIOENCODING="utf-8"
python -m pytest test_tools_*.py test_agents_*.py test_orchestrator_router.py test_api_smoke.py test_hitl_persist.py test_events_extended.py test_material_bom.py -q
```

近期：**约 60+ passed**（含 Phase 0–3 单测与冒烟）。

---

## 9. 前端与多智能体的衔接

| 功能 | 位置 |
|------|------|
| 常规排程 | APS →「开始排程计算」→ `/api/run` |
| 物料预检 | APS →「物料预检」→ `MaterialCheckDialog` |
| **排程落库 HITL** | APS →「排程并待确认落库」→ `/api/agents/scheduling/run`（或兼容 `/pipeline/run`） |
| 方案对比 / 交期 | 助手内 `AgentResultCards` 展示对比表与交期风险 |
| 自然语言排程 | 仍可用 `/api/agent/schedule` |

**未做：** whatif/commitment 独立报表页（助手内已有结果卡片）。

**未补齐（菜单已上线）：**

| 模块 | 已有 | 待做 |
|------|------|------|
| 生产看板 · 数字孪生 | 快照 API + 表格 + 2s 轮询；执行态机台状态 | 三维场景、与所选计划/甘特同屏、AGV/人员动画、与物流/MES 深度联动 |
| 资源管控 · AGV 物流 | `/api/logistics/generate` 两阶段全局分配 + 任务入库；`/api/logistics/dispatch` 派发；`/api/logistics/complete` 完成；`/api/logistics/batches` 批次列表；`/api/logistics/tasks` 批次任务查询（分页） | 路径可视化、重排后自动刷新任务、看板级 KPI（准时率/空驶率） |

**已做：** Session/HITL Mongo（`.env` 设 `SESSION_STORE=mongo`）。

**已做：** `/new-ui/assistant` 智能助手（Orchestrator 对话 + HITL）。

---

## 10. 已知限制与后续建议

1. **LLM**：主路径 GLM（Router / Plan / Parse）；失败时规则回退（`LLM_FALLBACK=rule`）。
2. **Session**：`SESSION_STORE=mongo` 时落库；`memory` 时进程重启丢失。
3. **HITL token**：与 Session 共用 `SESSION_STORE`，mongo 模式重启仍有效（15 分钟 TTL）。
4. **权限审计（P5-D）**：最小 `audit_log` 已接；完整 RBAC 待定。
5. **HFSP**：仍搁置。
6. **路由验收**：`tests/test_router_phrases.py`（20 条话术，规则路径）。

**建议下一步：** 数字孪生三维与同屏联动、AGV 物流路径可视化与KPI看板、whatif/commitment 独立报表页、MCP 生产接入、完整权限模型。

---

## 11. 关键代码路径

```text
src/metaforge/
  tools/           # 共享 Tool
  agents/          # 6 × AgentRunner
  orchestrator/    # router.py
  services/        # event_reschedule.py, persist_store.py
  agent/           # 原 SchedulingAgent（NL 解析）
frontend/src/
  components/PersistConfirmDialog.vue
  views/APSView.vue
tests/main.py      # 所有 HTTP 入口
```
